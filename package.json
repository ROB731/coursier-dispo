{
  "name": "dispo-coursier",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "build:api": "cd apps/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "typecheck": "cd apps/api && npm run typecheck && cd ../web && npm run typecheck"
  }
}
